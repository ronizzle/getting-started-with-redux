// REDUCER
function counter(currentState, action) {
    switch(action.type) {
        case 'ADD':
            break;

        default:
            break;
    }
}


var store = Redux.createStore(counter(counter))

// ACTIONS
{type: 'ADD'}